# Lapify
Lapify is a tool that is meant to automate laptop registry on school gates .  It registers new student , assigning each a unique id that is converted to a qr code . Each student prints the qr code which is scanned at the gate. On scanning , it logs the student and logs them out when they leave the compound . 
